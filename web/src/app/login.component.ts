import {Component,inject,signal} from '@angular/core';import {FormsModule} from '@angular/forms';import {Router} from '@angular/router';import {AuthService} from './auth.service';
@Component({selector:'app-login',standalone:true,imports:[FormsModule],template:`<main class="auth-page"><section class="welcome"><a class="brand">Drupal<span>Spark</span> ✦</a><div><span class="eyebrow">CERTIFICATION QUEST</span><h1>Learn Drupal.<br><em>Level up.</em></h1><p>Focused practice, lively feedback, and a streak worth protecting.</p></div><div class="mascot">🧠<i>✦</i></div></section><section class="login-card"><div class="tabs"><button [class.active]="mode()==='login'" (click)="mode.set('login')">Log in</button><button [class.active]="mode()==='create'" (click)="mode.set('create')">Create account</button></div><h2>{{mode()==='login'?'Welcome back 👋':'Join the quest ✨'}}</h2>@if(mode()==='create'){<label>Name<input [(ngModel)]="name" autocomplete="name" placeholder="Your name"></label>}<label>Email<input [(ngModel)]="email" type="email" autocomplete="email" placeholder="you@example.com"></label><label>Password<input [(ngModel)]="password" type="password" placeholder="At least 6 characters"></label>@if(error()){<p class="form-error">{{error()}}</p>}@if(message()){<p class="form-success">{{message()}}</p>}<button class="primary wide" [disabled]="busy()" (click)="submit()">{{busy()?'One moment…':(mode()==='login'?'Log in':'Create account')}}</button><div class="divider"><span>or</span></div><button class="google wide" [disabled]="busy()" (click)="google()"><b>G</b> Continue with Google</button>@if(mode()==='login'){<button class="link" (click)="reset()">Forgot password?</button>}</section></main>`})export class LoginComponent{private auth=inject(AuthService);private router=inject(Router);mode=signal<'login'|'create'>('login');busy=signal(false);error=signal('');message=signal('');name='';email='';password='';async submit(){this.busy.set(true);this.error.set('');try{if(this.mode()==='create'){if(this.password.length<6)throw new Error('Password must contain at least 6 characters.');await this.auth.create(this.name,this.email,this.password);}else await this.auth.login(this.email,this.password);await this.router.navigate(['/home']);}catch(e:any){this.error.set(this.friendly(e));}finally{this.busy.set(false);}}async google(){this.busy.set(true);try{await this.auth.google();await this.router.navigate(['/home']);}catch(e:any){this.error.set(this.friendly(e));}finally{this.busy.set(false);}}async reset(){if(!this.email){this.error.set('Enter your email first.');return;}try{await this.auth.reset(this.email);this.message.set('Password reset email sent.');}catch(e:any){this.error.set(this.friendly(e));}}private friendly(e:any){const c=String(e?.code||'');if(c.includes('invalid-credential'))return'Email or password is incorrect.';if(c.includes('email-already'))return'An account already uses this email.';if(c.includes('popup-closed'))return'Google sign-in was cancelled.';return e?.message||'Something went wrong.';}}
