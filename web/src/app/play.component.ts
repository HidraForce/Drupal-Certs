import {HttpClient} from '@angular/common/http';import {Component,OnDestroy,inject,signal} from '@angular/core';import {FormsModule} from '@angular/forms';import {RouterLink} from '@angular/router';import {APP_CONFIG} from './app.component';import {AuthService} from './auth.service';
interface Question{id:string;domain:string;prompt:string;options:string[]}interface Result{correct:boolean;answer:number;explanation:string}
@Component({selector:'app-play',standalone:true,imports:[FormsModule,RouterLink],template:`<header class="nav"><a class="brand" routerLink="/home">Drupal<span>Spark</span> ✦</a><a routerLink="/home">Exit</a></header><main class="game-page">@if(stage()==='setup'){<section class="setup-card"><span class="big-icon">🎯</span><span class="eyebrow">GAME SETUP</span><h1>Build your challenge</h1><p>Every question has 30 seconds. Stay sharp—the timer moves automatically.</p><label>Number of questions<select [(ngModel)]="count"><option [ngValue]="5">5 · Quick spark</option><option [ngValue]="10">10 · Study break</option><option [ngValue]="20">20 · Full focus</option></select></label><button class="primary wide" (click)="start()">Start challenge →</button></section>}@else if(stage()==='loading'){<section class="setup-card"><span class="big-icon pulse">🧠</span><h2>Shuffling your questions…</h2></section>}@else if(stage()==='playing'&&current;as q){<section class="game-top"><div><span>Question {{index()+1}} of {{questions().length}}</span><div class="progress"><i [style.width.%]="((index()+1)/questions().length)*100"></i></div></div><div class="timer" [class.danger]="seconds()<=10">⏱ {{seconds()}}s</div></section><section class="quiz-card"><div class="quiz-head"><span>{{q.domain}}</span><strong>Score {{score()}}</strong></div><h2>{{q.prompt}}</h2><div class="answers">@for(option of q.options;track option;let i=$index){<button [class.selected]="selected()===i" [class.correct]="result()&&result()?.answer===i" [class.wrong]="result()&&selected()===i&&!result()?.correct" (click)="choose(i)"><b>{{['A','B','C','D'][i]}}</b>{{option}}</button>}</div>@if(result();as f){<div class="feedback" [class.win]="f.correct"><span>{{f.correct?'🎉':'💡'}}</span><div><strong>{{f.correct?'Brilliant!':'Learn this one'}}</strong><p>{{f.explanation}}</p></div></div>}<div class="actions">@if(!result()){<button class="primary" [disabled]="selected()===null" (click)="check()">Check answer →</button>}@else{<button class="primary" (click)="next()">{{index()+1===questions().length?'See results':'Next question'}} →</button>}</div></section>}@else if(stage()==='done'){<section class="setup-card result-card"><span class="big-icon">{{score()/questions().length>=.7?'🏆':'🌱'}}</span><span class="eyebrow">SESSION COMPLETE</span><h1>{{score()}} / {{questions().length}}</h1><p>{{score()/questions().length>=.7?'Excellent work—your Drupal instincts are growing.':'Every miss is a map. Review and try again!'}}</p><button class="primary wide" (click)="restart()">Play again</button><a routerLink="/home">Back home</a></section>}@else{<section class="setup-card"><h2>Could not load questions</h2><p>Check the Quiz API URL and try again.</p><button class="primary" (click)="restart()">Try again</button></section>}</main>`})export class PlayComponent implements OnDestroy{private http=inject(HttpClient);private config=inject(APP_CONFIG);private auth=inject(AuthService);stage=signal<'setup'|'loading'|'playing'|'done'|'error'>('setup');count=5;questions=signal<Question[]>([]);index=signal(0);selected=signal<number|null>(null);result=signal<Result|null>(null);score=signal(0);seconds=signal(30);private timer?:number;get current(){return this.questions()[this.index()];}start(){this.stage.set('loading');this.http.get<Question[]>(`${this.config.quizApi}/v1/questions`).subscribe({next:q=>{this.questions.set([...q].sort(()=>Math.random()-.5).slice(0,Math.min(this.count,q.length)));if(!this.questions().length){this.stage.set('error');return;}this.stage.set('playing');this.startTimer();},error:()=>this.stage.set('error')});}choose(i:number){if(!this.result())this.selected.set(i);}async check(){const q=this.current,a=this.selected();if(!q||a===null)return;clearInterval(this.timer);const t=await this.auth.token();this.http.post<Result>(`${this.config.quizApi}/v1/questions/${q.id}/check`,{answer:a},{headers:{Authorization:`Bearer ${t}`}}).subscribe({next:r=>{this.result.set(r);if(r.correct)this.score.update(v=>v+1);},error:()=>this.next()});}next(){if(this.index()+1>=this.questions().length){void this.finish();return;}this.index.update(v=>v+1);this.selected.set(null);this.result.set(null);this.seconds.set(30);this.startTimer();}restart(){clearInterval(this.timer);this.stage.set('setup');this.index.set(0);this.score.set(0);this.selected.set(null);this.result.set(null);}ngOnDestroy(){clearInterval(this.timer);}private startTimer(){clearInterval(this.timer);this.timer=window.setInterval(()=>{this.seconds.update(v=>v-1);if(this.seconds()<=0){clearInterval(this.timer);this.next();}},1000);}private async finish(){clearInterval(this.timer);this.stage.set('done');try{const t=await this.auth.token();const h={headers:{Authorization:`Bearer ${t}`}};const p=await this.http.get<any>(`${this.config.progressApi}/v1/progress`,h).toPromise();await this.http.post(`${this.config.progressApi}/v1/progress`,{correct:(p?.correct||0)+this.score(),total:(p?.total||0)+this.questions().length,streak:Math.max(1,p?.streak||0)},h).toPromise();}catch{}}}
